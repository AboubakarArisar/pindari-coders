/**
 * Resource entries for the public shelf. A future authenticated admin can manage this shape.
 * @type {Array<{id: string, name: string, description: string, image: string, url: string}>}
 * Example shape: { id: 'library-id', name: 'Library name', description: 'What it helps with', image: '/resource-image.png', url: 'https://official-library-site.example' }
 */
export const resources = [];
