import Link from 'next/link';
import { roadmaps } from '../../lib/roadmaps';
export const metadata = { title: 'Learn by domain', description: 'Choose Frontend, Backend, React, or Next.js with topics, resources, and a project to build.' };
export default function LearnPage() { return <main className="wrap section"><div className="page-heading"><p className="eyebrow">A LITTLE DIRECTION</p><h1>what do you<br />want to <span className="serif-word">build?</span></h1><p>Choose a domain. Find the foundations, the next step, and something useful to make.</p></div><div className="domain-grid">{roadmaps.map((domain,index) => <Link key={domain.slug} href={`/learn/${domain.slug}/`} className="domain-card"><div className="domain-card-top"><span className="roadmap-symbol" aria-hidden="true">{domain.symbol}</span><span className="eyebrow">0{index+1} / {domain.category.toUpperCase()}</span></div><h2>{domain.title}</h2><p>{domain.summary}</p><div className="domain-outcome"><span className="eyebrow">YOU’LL WORK TOWARD</span><p>{domain.outcome}</p></div><span className="roadmap-card-bottom">explore {domain.title} <span aria-hidden="true">↗</span></span></Link>)}</div><p className="learning-note">New to coding? Start with Frontend. Already comfortable with JavaScript? Explore React or Backend. <Link href="/roadmaps/">Browse all roadmaps ↗</Link></p></main>; }
