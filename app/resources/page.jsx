import Link from 'next/link';
import { resources } from '../../lib/resources';
export const metadata = { title: 'Resources', description: 'A growing collection of useful libraries, tools, and resources for making things.' };
export default function ResourcesPage() {
  return <main className="wrap section"><div className="page-heading"><p className="eyebrow">THE RESOURCE SHELF</p><h1>good tools.<br /><span className="serif-word">less friction.</span></h1><p>Libraries, tools, and useful finds. A place to discover what helps, understand what it does, and go straight to the source.</p></div>{resources.length ? <div className="resource-grid">{resources.map(resource => <a className="resource-card" key={resource.id} href={resource.url} target="_blank" rel="noopener noreferrer"><img src={resource.image} alt="" width="640" height="360" /><div><h2>{resource.name} ↗</h2><p>{resource.description}</p><span className="text-link">visit resource ↗</span></div></a>)}</div> : <section className="resource-empty"><span className="shelf-symbol" aria-hidden="true">[ + ]</span><p className="eyebrow">A SHELF WORTH FILLING CAREFULLY</p><h2>the first finds are still on their way.</h2><p>We’re making room for useful libraries and tools, with a clear explanation of what each one helps you do. Check back for our first additions.</p><Link className="button primary" href="/lab/">explore the lab meanwhile ↗</Link></section>}</main>;
}
